<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_turkiz"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_turkiz_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Azft2ichesstwoplayer20"
        tools:targetApi="31">
        <activity
            android:name=".FirstActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".GameMainActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".GameStartActivity" />
        <activity android:name=".ChessRulesActivity" />
        <activity android:name=".DevelopersActivity" />
        <activity android:name=".GameTiedActivity" />

    </application>

</manifest>